import { UiPageWrapperHeaderColorDirective } from './ui-page-wrapper-header-color.directive';

describe('UiPageWrapperHeaderColorDirective', () => {
    it('should create an instance', () => {
        const directive = new UiPageWrapperHeaderColorDirective();

        expect(directive).toBeTruthy();
    });
});
