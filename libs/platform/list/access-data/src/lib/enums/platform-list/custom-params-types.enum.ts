export enum CustomParamsTypesIdEnum {
    Include = 1,
    Exclude = 2
}

export enum CustomParamsTypesTranslateEnum {
    Include = 'offers_page.custom_parameters.types.include',
    Exclude = 'offers_page.custom_parameters.types.exclude'
}
